spring:
  jackson:
    serialization:
      indent-output: true

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://ecommerce-microservice-backend-app-service-discovery-container-1:8761/eureka
  instance:
    hostname: ${EUREKA_INSTANCE:localhost}
